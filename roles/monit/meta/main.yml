dependencies:
  - role: nginx
    when: 'jailed != true'
    vars:
      servers:
        - name: '{{ inventory_hostname }}'
          service_name: monit
          port: 2812
          tls: true
          proxy_protocol: false
          locations:
            - path: /
              proxy:
                pass: http://unix:/var/run/monit.sock
