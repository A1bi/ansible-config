- hosts: jail_hosts
  roles:
    - name: monit

- hosts: jails
  tasks:
    - name: Set jail name
      set_fact:
        jail_name: '{{ ansible_hostname|replace("-", "_") }}'

- hosts: nsd_servers
  roles:
    - nsd
